// condição if e else

const velocidade = 80;

if(velocidade <= 80){
    console.log(`Sua velocidade é ${velocidade} você esta na velocidade permitida`);
}

else{
    console.log(`Sua velocidade é ${velocidade} voce esta acima da velocidade Multa!!!`); 
}